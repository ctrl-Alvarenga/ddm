<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP03EX2">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AlunoNomeActivity"
            android:exported="false"
            android:label="@string/title_cadastro_aluno" />
        <activity
            android:name=".AlunoMatriculaActivity"
            android:exported="false"
            android:label="@string/title_matricula_aluno" />
        <activity
            android:name=".ImcEntradaActivity"
            android:exported="false"
            android:label="@string/title_calculadora_imc" />
        <activity
            android:name=".ImcResultadoActivity"
            android:exported="false"
            android:label="@string/title_resultado_imc" />
    </application>

</manifest>
